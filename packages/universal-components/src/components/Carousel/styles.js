import { StyleSheet } from 'react-native'; // eslint-disable-line

export default _width =>
  StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
    cardContainer: {},
  });
